# Contributing

1. 使用仓库声明的 Node 与 pnpm 版本。
2. 从 `master`/`main` 创建短生命周期分支。
3. 一个提交只处理一个清晰问题，避免夹带格式化全仓等无关改动。
4. 新功能应补充对应测试；修复缺陷应优先添加回归用例。
5. 提交前运行 `pnpm check && pnpm build`。
6. 不提交 `.env.*`、密钥、私网地址、构建产物或额外锁文件。
7. 修改公共组件、路由协议、请求封装或第三方 patch 时，在 PR 中说明兼容性影响与回滚方式。
